# form_validator
A simple Email and Password Validation Form built using HTML, CSS, and JavaScript. It validates user input using Regular Expressions (Regex) and displays error messages for invalid credentials.
